Status marker inside tables, cards and console rows.

```jsx
<Badge tone="positive" dot>Complete</Badge>
<Badge tone="caution">In review</Badge>
```

Soft fill by default. Use `solid` only when the state has to be unmissable. One
badge per row.
