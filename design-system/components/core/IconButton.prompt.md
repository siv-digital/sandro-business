Icon-only control for toolbars, cards and app chrome.

```jsx
<IconButton name="Bookmark" label="Save" variant="outline" />
```

`label` is required and becomes both `aria-label` and `title`. `quiet` by default; `outline` when it needs to read as a control; `solid` sparingly.
