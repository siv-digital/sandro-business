A hairline — the system's primary separator, used far more than shadow.

```jsx
<Rule tone="accent" animated />
<Rule orientation="vertical" />
```

`tone="gradient"` is the aqua→azure→sand keyline; use once per page at most. `animated` wipes it in from the left.
