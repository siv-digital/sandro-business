Category and filter tokens.

```jsx
<Tag active onClick={() => setFilter('all')}>All</Tag>
```

Uppercase, tracked, pill. Passing `onClick` makes it a button. Use for taxonomy, never for status — that's `Badge`.
