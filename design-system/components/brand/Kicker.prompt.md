Uppercase wide-tracked eyebrow above a headline — the brand's most-repeated typographic device.

```jsx
<Kicker>Practice infrastructure</Kicker>
```

`dash={false}` drops the leading aurora hairline (use inside cards, where the dash is noise). Keep to two or three words; never a sentence.
