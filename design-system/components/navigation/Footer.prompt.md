Titanium site footer.

```jsx
<Footer note="From the business you built to the wealth it becomes."
  columns={[{title:'The Journey',links:['The five phases','Tracks','Assessment']}]} />
```

Pass `disclosure` only when you have approved copy. Nothing renders when it is
unset.
