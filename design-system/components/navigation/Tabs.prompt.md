Underlined tab row for in-page and in-app view switching.

```jsx
<Tabs items={[{id:'all',label:'All'},{id:'q',label:'Quarterly'}]} value={tab} onChange={setTab} />
```

Four tabs or fewer. Use `Tag` for filtering a collection, not `Tabs`.
