Native select in the brand's field chrome.

```jsx
<Select label="Revenue" options={['Under $10M','$10M to $50M','$50M+']} />
```

Options accept strings or `{ value, label }`. Keep lists under about 12. Beyond
that, use a search field.
