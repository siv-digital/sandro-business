Text field.

```jsx
<Input label="Work email" type="email" placeholder="you@firm.com" hint="We reply within one business day." />
```

`error` replaces `hint` and flips the border to critical. `prefix`/`suffix` for units and currency. Labels are sentence case.
